import { View } from "react-native";

type Props = {
    titre: string;
    profondeur: number;
    temps: number;
    gazFond: string;
}

export default function PlongeeCard({ titre, profondeur, temps, gazFond }: Props) {
    return (
        <View></View>
    )
}